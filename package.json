{
  "name": "dazu-server",
  "version": "1.0.0",
  "description": "DAZU mesh messaging server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
